{
  "compilerOptions": {
      "sourceMap": true, //为true时可以在.ts上面打断点
      "outDir": "./out", //生成的js和js.map的目录
  },
  "include": [ // 需要编译的ts文件一个*表示文件匹配**表示忽略文件的深度问题
      "./src/*.ts",
      "./src/**/*.ts"
],
"exclude": [ 

]
}